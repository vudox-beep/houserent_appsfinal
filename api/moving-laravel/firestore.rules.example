rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Laravel (Admin SDK) writes; apps read only.
    match /moving_bookings/{bookingId} {
      allow read: if true;
      allow write: if false;
    }
  }
}
